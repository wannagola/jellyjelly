# Supabase 를 연결하면 로그인과 기기 간 동기화가 켜진다.
# 비워두면 지금처럼 기기 안에만 저장된다.
#
# Supabase 대시보드 → Project Settings → API 에서 가져온다.
# anon 키는 브라우저에 노출되는 게 정상이다. 데이터는 RLS 가 막는다.
# service_role 키는 절대 여기 넣지 않는다.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
